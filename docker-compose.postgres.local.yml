services:
  postgres:
    image: postgres:16
    container_name: shivakala_postgres_local
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: shivakala
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_local_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d shivakala"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_local_data:
